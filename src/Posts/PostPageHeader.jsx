import React from 'react';

// Super simple. Just render an h1 with some text.
// Absolutely nothing going on here.
// This could be changed to accept the text to render
// as a child and then whichever component includes
// this one could send in any text it wanted.
const PostPageHeader = () => 
    <h1>Posts</h1>

export default PostPageHeader;